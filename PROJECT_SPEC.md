# 大型公共建筑钢构件耐火保护证据闭环

## 项目目标

面向大型公共建筑的一批钢梁、钢柱，从基材交接到耐火保护验收建立可恢复、可审计的质量闭环。系统以“构件—受火面—测点网格—涂层遍次”为只追加证据图，在任务锁定时固化规则摘要、空间范围、材料批次、施工计划、设备校准、养护窗口和验收阈值。所有业务命令通过持久化事务、幂等操作号和逻辑时钟执行；受火面施工仅能形成锁定遍次的连续前缀，返涂通过新代次追加证据，旧读数永不覆盖。服务采用 Go HTTP API，建议使用 SQLite 或 PostgreSQL 兼容的关系模型与事务接口，启动时从快照及事件日志恢复未终结任务、材料余额、租约和待重试设备调用。生产实现控制在20—30个Go文件、2000—3000有效行并以约2500行为目标，划分至少4个有实质职责的包；公开测试不少于4个测试文件和12个确定性用例。仓库包含go.mod、可编译服务入口、数据库迁移、健康检查，以及支持linux/amd64和linux/arm64的多阶段Docker构建。

## 端到端业务流程

1. 建档锁定：录入楼层、防火分区、钢梁或钢柱编号、整数截面尺寸、受火面全集与边界、受火面邻接图、喷涂区、设计耐火极限、涂料和底涂批次；使用公开整数规则计算截面系数与目标干膜厚度，校验规则摘要及材料证明版本后，固化任务代次、遍次计划、测点网格、针刺位置、粘结试件、养护窗口、设备校准摘要和阈值。
2. 开工准备：按依赖顺序完成基面交接确认、基层处理摘要、底涂相容性核验和材料启封。领用每批涂料时，在同一事务内从整数克可消耗池扣减计划用量并获取喷涂机限时互斥租约；同一操作号和同一内容重试返回原结果，内容变化返回幂等冲突。
3. 分遍施工与养护：每个受火面仅允许写入下一锁定遍次，形成连续施工前缀；记录喷涂区、材料批次、面积、领用克数、合理损耗、回收克数、湿膜梳测点及定点湿膜值。遍次结束后采集逻辑时间排序的温湿度轨迹，只有完整覆盖锁定窗口才允许下一遍或检测。
4. 检测与覆盖核验：原子获取测厚仪、拉拔仪等设备租约，记录干膜厚度网格、针刺测厚、空鼓、开裂、脱落和拉拔力。采用整数定点运算计算累计厚度、离散度、单位面积用量与粘结强度；设备拒绝、断连、超时或格式错误只追加待重试调用，不生成有效读数或推进状态。
5. 返涂复验：任一厚度不足、离散度越界、空鼓、开裂、脱落或粘结不足触发确定性影响域算法，按同喷涂区、锁定邻接点及同材料批次求并集，并依楼层、防火分区、构件、受火面、测点排序。仲裁器一次性创建下一任务代次及返涂、养护、厚度和粘结复验集合；新代次重新覆盖受影响遍次和测点，旧代次证据保留但迟到回执不能改变当前结论。
6. 独立复核与终局：两名不同且资格有效的人员分别对材料守恒、施工前缀、养护覆盖、厚度覆盖、粘结证据和所有返涂代次进行复核。验收、质量隔离和取消共享单写终局屏障，并发请求中仅一个提交；验收成功生成与任务、当前代次及证据根摘要绑定的唯一凭据。

## 核心组件与职责

1. 构件与防火材料规则目录：管理楼层、防火分区、构件、截面、受火面边界与邻接图、耐火等级、涂料和底涂相容规则、材料证明、公开整数计算规则及带版本摘要的目录快照。
2. 涂层验收任务聚合：负责任务创建与锁定、只追加证据图、任务代次、依赖状态机、幂等命令账本、稳定错误排序、事务边界、持久化快照和启动恢复。
3. 材料用量及设备租约管理器：维护材料批次启封状态及整数克质量账，原子处理领用、回收、合理损耗；管理喷涂机、湿膜梳、电子秤、温湿度探头、测厚仪和拉拔仪的逻辑时钟限时互斥租约。
4. 分遍施工与养护证据记录器：校验基面、底涂和遍次连续前缀，记录湿膜、面积用量、施工区和材料绑定；采集养护轨迹并管理脚本化设备调用、确定重试次数及有效证据晋级。
5. 厚度粘结复验及终局仲裁器：执行干膜、针刺、离散度、粘结强度计算，生成缺陷影响域和新代次复验计划，验证双人独立复核，并以单写屏障裁定验收、质量隔离或取消。
6. Go HTTP API：提供JSON命令与查询端点、操作号和版本前置条件、稳定错误响应、证据分页查询、健康检查及恢复状态；不包含独立前端。

## 领域规则与不变量

1. 任务锁定后，楼层、防火分区、构件与受火面全集、构件截面、耐火极限、材料及底涂批次、遍次、测点、试件、邻接图、养护窗口、设备校准摘要、阈值和任务代次均不可原地修改；变更只能创建明确的新代次。
2. 截面系数和目标厚度使用公开的整数查表与有理数规则计算，统一规定缩放因子和半数远离零舍入；退化尺寸、负数、除零、超长输入及任一步乘加溢出导致整条命令回滚。
3. 受火面必须属于锁定构件且全集完整；测点标识在受火面内唯一、整数坐标落入边界并存在锁定的喷涂区归属。所有输出原因按楼层、防火分区、构件、受火面、测点、代次升序排列。
4. 涂料批次满足“启封量+后续补入量=累计领用量+可消耗余额+累计回收量+批准合理损耗量”，全部值为非负整数克。过期或证明摘要陈旧的批次不可启封或领用；材料扣减与必需设备租约必须在一个事务内成功或全部失败。
5. 每个受火面、每个代次的有效喷涂证据必须是锁定遍次从第一遍开始的连续前缀。基面确认、相容底涂、上一遍完成、规定养护闭合和有效租约构成下一遍的前置条件；重复遍次、跳遍、错误构件、错误代次和过期开放时间均拒绝。
6. 湿膜、干膜、面积用量、温湿度时长、拉拔力和粘结强度均采用带明确量纲与缩放因子的int64定点值。仪器失败仅写入含逻辑时间、请求摘要、故障类型、当前次数和确定下次重试时间的调用记录；达到上限后转人工处置，但不得合成读数。
7. 缺陷影响域为缺陷测点所在喷涂区、锁定邻接图中规定距离内测点，以及与缺陷证据绑定的同材料批次受影响点的确定性并集。返涂只能创建递增代次，旧证据保持可查；所有写入必须匹配当前代次和聚合版本。
8. 终局值仅可为空、已验收、质量隔离或已取消之一。验收要求质量守恒、全部施工前缀和养护窗口闭合、厚度与粘结达标、返涂复验完成及两名不同合格人员独立复核；终局一旦写入不可撤销或替换。

## 数据模型与持久化

1. CatalogSnapshot：目录版本、规则摘要、材料证明摘要、底涂相容矩阵、设备校准摘要、公开厚度规则；锁定任务仅引用不可变快照。
2. Task、TaskGeneration、Member、ExposedFace、GridPoint、AdjacencyEdge、SprayZone、CoatPlan、Specimen：描述任务范围、当前代次、构件截面和耐火参数、受火面边界、测点网格、针刺点、邻接关系、遍次及试件。
3. EvidenceEvent：只追加事件，包含聚合序号、逻辑时间、操作号、任务代次、证据类型、规范化载荷摘要和前一事件哈希；Snapshot记录可重建状态及最后事件序号。
4. MaterialBatch、MaterialLedgerEntry：记录批次启封、领用、回收、损耗和余额；每条账目关联操作号、任务、代次、遍次与喷涂区，并以事务约束保证质量守恒。
5. Equipment、Lease、DeviceInvocation：记录设备种类、校准摘要、租约所有者、逻辑到期点，以及设备调用的请求摘要、脚本结果、重试状态和有效读数引用。
6. SurfacePreparation、CoatApplication、WetFilmReading、CuringSample、DryFilmReading、NeedleReading、BondSpecimenResult、Defect、RecoatScope、Review、TerminalDecision、AcceptanceCredential：保存施工、检测、返涂、复核和唯一终局凭据；旧代次记录仅追加且不可删除。
7. IdempotencyRecord：以调用方和操作号唯一，保存规范化请求摘要、处理中或已提交状态、响应摘要及关联事件区间；同键异内容稳定返回冲突，崩溃恢复后可判定未决事务。

## 公开接口

1. POST /v1/tasks、POST /v1/tasks/{id}/lock：创建并锁定任务；锁定响应返回目录摘要、计算出的截面系数和目标厚度、任务代次及聚合版本。
2. POST /v1/tasks/{id}/commands：统一提交基面确认、材料启封与领用、底涂、遍次开始或完成、养护采样、设备调用与检测证据；请求头携带Idempotency-Key，正文携带expected_version、generation和逻辑时间。
3. POST /v1/tasks/{id}/defects/evaluate、POST /v1/tasks/{id}/recoat-generations：确定性计算缺陷影响域并原子创建返涂代次；预览与提交使用同一规范化算法和范围摘要。
4. POST /v1/tasks/{id}/reviews、POST /v1/tasks/{id}/terminal-decisions：提交独立复核以及验收、质量隔离或取消裁定；成功验收返回唯一凭据编号和证据根摘要。
5. GET /v1/tasks/{id}、GET /v1/tasks/{id}/evidence、GET /v1/tasks/{id}/material-balance、GET /v1/tasks/{id}/recoat-scope：查询当前投影、按稳定游标分页的证据图、材料守恒明细和有序复验范围。
6. GET /health/live、GET /health/ready：分别报告进程存活及数据库迁移、事件恢复和未决幂等记录检查结果；所有拒绝统一返回code、message、ordered_reasons、operation_id和current_version。

## 失败边界

1. 目录或输入校验失败使用稳定错误码，如CATALOG_STALE、MEMBER_MISMATCH、FIRE_RATING_MISMATCH、FACE_INCOMPLETE、POINT_DUPLICATE、PRIMER_INCOMPATIBLE、FIXED_POINT_OVERFLOW；失败不得创建任务锁定事件。
2. 施工状态拒绝使用COAT_OUT_OF_ORDER、COAT_DUPLICATE、GENERATION_STALE、MATERIAL_EXPIRED、OPEN_TIME_EXPIRED、CURING_WINDOW_INVALID；事务回滚后不留下施工前缀、有效证据或材料变动。
3. 资源竞争使用MATERIAL_INSUFFICIENT、LEASE_BUSY、LEASE_EXPIRED。材料扣减、租约获取和施工命令共用数据库事务；唯一约束或版本冲突映射为确定错误，不进行隐式部分重试。
4. 设备故障DEVICE_REJECTED、DEVICE_DISCONNECTED、DEVICE_TIMEOUT、DEVICE_FORMAT_INVALID只提交待重试调用事件；租约过期后返回LEASE_EXPIRED，任何故障均不得生成读数、检测结论或验收资格。
5. 操作号重放先比较规范化请求摘要；相同内容返回已存响应，不重复扣料或写事件，不同内容返回IDEMPOTENCY_CONFLICT。进程在提交响应前崩溃时，重启从记录恢复并返回唯一结果。
6. 返涂代次竞争返回GENERATION_CONFLICT，旧代次迟到数据返回GENERATION_STALE；验收、隔离、取消通过终局唯一行和条件更新竞争，失败方返回TERMINAL_ALREADY_DECIDED且不产生凭据。

## 验收标准

1. 锁定操作原子固化指定防火分区、构件及受火面全集、截面与耐火参数、材料和底涂批次、遍次、网格、试件、养护窗口、设备摘要、阈值和任务代次；公开整数规则得到精确截面系数与目标厚度，非法尺寸、除零或溢出时数据库无任务锁定残留。
2. 受火面和测点完整、唯一且落入构件范围；基面、相容底涂、遍次喷涂、养护和检测严格按依赖推进，每个受火面只形成连续遍次前缀，错误构件、重复或跳遍、过期开放时间及错误代次均返回稳定且有序的拒绝原因。
3. 材料启封、领用、回收和合理损耗在任意成功序列后保持整数克质量守恒；并发领料与喷涂机或检测设备租约获取至多一个成功，失败事务不遗留扣减或租约；相同幂等请求不重复生效，异内容冲突可复现。
4. 所有面积用量、湿膜、累计干膜、养护窗口、拉拔力和粘结强度按规定定点尺度与舍入规则产生精确结果；负值、超长、除零和int64乘加溢出整体拒绝且不写入有效证据。
5. 脚本化探头、电子秤、测厚仪和拉拔仪的拒绝、断连、超时及格式错误仅生成按逻辑时钟可预测的待重试记录；成功重试只晋级一次有效读数，租约过期或超过次数上限不推进施工或检测状态。
6. 缺陷按锁定喷涂区、邻接图和同材料批次一次生成排序稳定的返涂复验集合；新代次覆盖全部受影响遍次与测点，旧读数永久可查，旧代次迟到回执及并发建代均不能改变当前结论。
7. 服务重启后可由持久化事件和快照恢复任务代次、施工前缀、材料余额、有效租约、待重试调用、复核与终局；模拟提交后断开再重试不会重复扣料、重复事件或重复凭据。
8. 仅当所有质量条件闭合且两名不同合格人员独立复核后生成唯一验收凭据；使用同步屏障并发提交验收、质量隔离和取消时恰好一个终局成功，其余获得同一已裁定结果。

## 确定性测试场景

1. catalog_lock_test.go：使用虚构楼栋A座、固定H型钢梁柱尺寸和公开厚度表，断言截面系数、目标厚度、规则摘要及完整锁定快照。
2. catalog_lock_test.go：分别输入退化截面、负尺寸、除零规则和乘加溢出，断言FIXED_POINT_OVERFLOW或对应稳定码且锁定事务无残留。
3. task_graph_test.go：提交缺失受火面、跨构件测点和重复测点，断言原因按楼层、防火分区、构件、受火面、测点、代次排序。
4. task_graph_test.go：完成基面和相容底涂后按三遍计划施工，断言每个受火面的有效前缀依次为0、1、2、3。
5. task_graph_test.go：尝试跳至第三遍、重复第二遍、使用错误构件和旧代次，断言无前缀推进且事件图无有效施工证据。
6. material_lease_test.go：以同步屏障让两个任务竞争同一批次最后一份克数及同一喷涂机，断言至多一个成功、材料守恒且失败方无租约。
7. material_lease_test.go：对领料命令进行同操作号同内容重试及异内容重试，断言前者返回原响应、后者IDEMPOTENCY_CONFLICT且账本仅有一次扣减。
8. fixedpoint_device_test.go：用固定面积、湿膜、干膜、养护序列和拉拔力验证半数远离零舍入、累计厚度、单位面积用量及粘结强度的精确整数结果。
9. fixedpoint_device_test.go：脚本依次返回超时、格式错误和成功值，推进逻辑时钟，断言确定重试次数与时间、前两次无读数、最终仅一个有效读数。
10. fixedpoint_device_test.go：在设备调用完成前推进时钟越过租约到期点，断言LEASE_EXPIRED且迟到结果不能晋级为检测证据。
11. recoat_terminal_test.go：构造厚度不足、相邻空鼓及同批次开裂，依据公开邻接图断言返涂影响域的精确有序并集和新代次覆盖计划。
12. recoat_terminal_test.go：新代次创建后提交旧代次测厚回执及两个并发建代请求，断言迟到回执隔离且仅一个新代次成立。
13. recoat_terminal_test.go：材料不守恒、养护缺口、厚度不足、粘结不足或仅一名复核人时分别拒绝验收，并断言无验收凭据。
14. recoat_terminal_test.go：两名不同合格人员复核后，以同步屏障并发验收、隔离和取消，断言唯一终局和至多一张验收凭据。
15. recovery_test.go：在材料与租约事务提交后、HTTP响应前模拟进程退出，重启并重放操作号，断言余额、租约、事件和响应均未重复。
16. recovery_test.go：从快照加尾部事件恢复开放任务、施工前缀、待重试设备调用和当前返涂代次，断言恢复前后查询投影字节级一致。

## 组件追踪关系

1. 构件与防火材料规则目录覆盖建档锁定流程、CatalogSnapshot及第1—2条验收标准，主要由catalog_lock_test.go和task_graph_test.go验证。
2. 涂层验收任务聚合承载只追加证据图、幂等、代次、稳定排序、事务与恢复，贯穿全部流程，重点对应第2、3、6、7条验收标准及recovery_test.go。
3. 材料用量及设备租约管理器覆盖材料启封、整数克质量守恒和互斥租约，对应第3条验收标准，由material_lease_test.go及设备租约过期用例验证。
4. 分遍施工与养护证据记录器覆盖基面、底涂、连续遍次前缀、养护轨迹和设备待重试，对应第4—6条验收标准，由task_graph_test.go和fixedpoint_device_test.go验证。
5. 厚度粘结复验及终局仲裁器覆盖定点检测、缺陷传播、返涂代次、双人复核与单写终局，对应第5、7、8条验收标准，由recoat_terminal_test.go验证。
6. Go HTTP API暴露全部命令、查询、稳定错误与健康检查，并通过各测试文件的HTTP层夹具验证幂等头、版本条件、错误结构、恢复就绪和确定响应。

## 独特性

本计划聚焦大型公共建筑钢梁柱耐火涂层从基材交接、分遍喷涂、养护与检测到返涂复验和终局验收的证据闭环，以受火面测点网格、材料整数克守恒、设备限时租约及不可覆盖的返涂代次为核心，不与既有高强螺栓、预制墙板、地下防水或其他农业食品类联检项目重复。
